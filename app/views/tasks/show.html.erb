<h1><%= @task.title %></h1>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>

<p><%= link_to 'index', tasks_path(@tasks) %></p>
<p><%= link_to 'Edit', edit_task_path(@task) %></p>
<p><%= link_to 'Delete',
delete_task_path(@task),
data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%></p>
